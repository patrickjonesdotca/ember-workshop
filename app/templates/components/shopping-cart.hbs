<h4>Shopping Cart</h4>
{{#if items}}
  <ul>
    {{#each product in items}}
    <li>
      <span {{action 'remove' product}}>[ X ]
      {{product-card in-cart=true model=product}}
      </span>
    </li>
    {{/each}}
  </ul>
{{else}}
  Nothing added yet.
{{/if}}
